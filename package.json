{
  "name": "japanote",
  "private": true,
  "scripts": {
    "build:data:kana": "node scripts/build-kana-data.mjs",
    "build:data": "npm run build:data:kana",
    "build:cache": "node scripts/cache-bust.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build": "npm run build:site",
    "dev": "node dev-serve.cjs",
    "start": "npm run dev"
  },
  "devDependencies": {
    "serve-handler": "^6.1.7"
  }
}
